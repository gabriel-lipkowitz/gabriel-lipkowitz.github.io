# Specify the source for downloading gems
source 'https://rubygems.org'

# Define the gems you want to install
gem 'jekyll', '~> 4.3.3'            # Jekyll for static site generation
# gem 'eventmachine', '1.2.7'         # Event-driven I/O framework
gem 'ffi', '1.16.3'                 # Foreign Function Interface (FFI) library
gem 'http_parser.rb', '0.8.0'       # HTTP parser for Ruby
gem 'eventmachine', github: 'eventmachine/eventmachine'
